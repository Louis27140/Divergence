services:
  caddy:
    image: caddy:2
    depends_on:
      - front
      - back
      - livekit
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
  turn:
    image: coturn/coturn:latest
    ports:
      - "3478:3478/udp"
      - "3478:3478/tcp"
      - "40000-40100:40000-40100/udp"
    command:
    - -n
    - --log-file=stdout
    - --listening-ip=0.0.0.0
    - --external-ip=<IP_ADDRESS>
    - --listening-port=3478
    - --min-port=40000
    - --max-port=40100
    - --fingerprint
    - --lt-cred-mech
    - --user=turnuser:turnpass
    - --realm=turn.<IP_ADDRESS>.nip.io
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: dbuser
      POSTGRES_PASSWORD: dbpass
      POSTGRES_DB: app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4:latest
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@local.io
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - db
    volumes:
      - pgadmin_data:/var/lib/pgadmin

  back:
    build: ./back
    environment:
      DATABASE_URL: postgres://dbuser:dbpass@db:5432/app
    depends_on:
      - db
    ports:
      - "3000:3000"
    volumes:
      - ./back:/app
      - /app/node_modules
    command: sh -c "if [ ! -x node_modules/.bin/nodemon ]; then npm install; fi && npm run dev"
    develop:
      watch:
        - action: sync+restart
          path: ./back/src
          target: /app/src
        - action: rebuild
          path: ./back/package.json
        - action: rebuild
          path: ./back/package-lock.json

  front:
    build: ./front
    depends_on:
      - back
    environment:
      VITE_USE_POLLING: "true"
      VITE_POLL_INTERVAL: "150"
    ports:
      - "5173:5173"
    volumes:
      - ./front:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0
    develop:
      watch:
        - action: sync+restart
          path: ./front/src
          target: /app/src
        - action: sync+restart
          path: ./front/index.html
          target: /app/index.html
        - action: sync+restart
          path: ./front/vite.config.ts
          target: /app/vite.config.ts
        - action: rebuild
          path: ./front/package.json
        - action: rebuild
          path: ./front/package-lock.json

  livekit:
    image: livekit/livekit-server:latest
    command: ["--config", "/livekit.yaml"]
    volumes:
      - ./livekit.yaml:/livekit.yaml:ro
    ports:
      - "7880:7880"
      - "7881:7881/udp"
      - "7882:7882"

volumes:
  pgdata:
  pgadmin_data:
  caddy_data:
  caddy_config:
